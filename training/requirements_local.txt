# Local training on RTX 4050 (6 GB VRAM) — Windows + CUDA 12.x
# Install order matters. Run:
#   pip install -r requirements_local.txt
#
# Pre-requisites:
#   1. NVIDIA driver >= 527
#   2. CUDA Toolkit 12.1  (https://developer.nvidia.com/cuda-downloads)
#   3. Python 3.10 or 3.11 (3.12 works too)
#   4. pip install torch --index-url https://download.pytorch.org/whl/cu121
#      (do this BEFORE running this file)

transformers>=4.46.0
accelerate>=1.0.0
peft>=0.13.0
bitsandbytes>=0.43.0
requests>=2.31.0
matplotlib>=3.7.0
pandas>=2.0.0
huggingface_hub>=0.25.0
