# Install order matters — do NOT pip install torch separately (Colab has it)
# Run in Colab: !pip install -r requirements.txt -q

unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl>=0.12.0
transformers>=4.46.0
accelerate>=1.0.0
peft>=0.13.0
requests>=2.31.0
matplotlib>=3.7.0
pandas>=2.0.0
huggingface_hub>=0.25.0
